# Celery Web Speak

Celery Web Speak 是围绕服务器、频道与实时语音协作组织的通信产品。本词汇表明确产品界面与设计文档中容易混淆的领域用语。

## 用户身份

**头像**：
用户账号上的图片标识，属于全局 User，与任何服务器无关；在用户设置的"账号"页上传与编辑。服务器成员名单中展示同一张用户头像，不存在按服务器区分的用户头像。头像可为动图，动画在所有展示处一致呈现。
_Avoid_: 服务器头像、成员头像、用户图标

**服务器图标**：
服务器(guild)入口上的可选图片标识，属于 guild 实体本身，由服务器管理员上传与移除；未设置时回退到服务器名称首字母圆角矩形。它与用户头像互不干涉——成员名单、参与者列表仍展示用户头像，而非该成员所在服务器的服务器图标。
_Avoid_: 服务器头像、guild 头像、服务器徽标、服务器图片

**简介**：
用户账号上的自我介绍文字，属于全局 User，与任何服务器无关；纯文本、最多 200 字符、可空，在用户设置的"账号"页编辑，跨所有服务器一致展示。
_Avoid_: 个人介绍、签名、about

**用户设置面板**：
用户管理自身账号信息与当前客户端偏好的统一设置空间；账号信息属于全局 User，客户端偏好只影响当前浏览器。
_Avoid_: 个人设置、账户设置、设置页

## 用户活动

**平台在线时长**：
用户携带活动连接到平台的累计时长，以整秒累加在全局 User 上；多端在线期间合并为一段、不重复计；在个人信息卡片上对同服成员公开。它只反映连接存在，不受在线状态影响——离开状态不暂停累计。
_Avoid_: 使用时长、在线时间、活跃时长、应用时长、平台使用时间

**服务器在线时长**：
按"活动服务器切片"把在线时长归到某服务器的累计量。当前未实现：现无 activeGuild 上报，无法判断用户在线期间以哪个服务器为活动服务器，故暂不做。
_Avoid_: 服务器使用时长、在服时长、服务器在线时间

**服务器语音时长**：
用户在指定服务器内作为语音参与者存在的累计整秒数，落在 (user, guild) 二元组上；跨频道跳转（同 guild 内 A→B）被当作同一段连续语音，跨 guild 各自独立计。它只包括"作为语音参与者"的时长，不含该服务器内非语音在线。所有卡片触发面（成员名单行、语音参与者行、消息作者）都携带 guild 上下文读取此值。
_Avoid_: 服务器通话时长、语音在线时长、频道语音时长、guild 语音时间

**服务器语音经验**：
用户在指定服务器内的语音参与折算的游戏化累积量，落在 (user, guild) 二元组上的 `voice_xp_total` 列，整数、与"服务器语音时长"各自独立累加、互不派生。累加点固定按 1 分钟语音 = 1 XP 折算（整除、不足 60s 的余数丢弃），不掺情况判断；未来多源折算（活动加成、事后扣减等）由旁路 UPDATE 该列实现，不进入语音 tick 路径。精度退化继承"服务器语音时长"：任何 < 60s 的语音会话整段不计 XP。
_Avoid_: 语音经验值、voice XP、服务器经验、经验点

**服务器语音等级**：
用户在指定服务器内的语音等级，是"服务器语音经验"的纯函数：到达 L 级所需总 XP = 10·L² + 40·L，给定 XP 取最大满足该不等式的 L。实时由后端按闭式解 + 回退检验算出，不持久化。仅携带 guild 上下文的卡片展示等级数字与"当前级进度 / 下一级所需"经验条。
_Avoid_: 语音级别、voice level、用户等级、成员等级

**等级颜色**：
个人信息卡片中用于表达"服务器语音等级"区间的视觉标识，从当前主题强调色逐级过渡到红色；达到 LV100 后保持红色，不代表服务器角色或权限。
_Avoid_: 等级徽章、角色颜色、权限颜色

## 在线状态

**在线状态**：
用户当前的在场状态，属于全局 User，跨所有服务器一致展示（成员名单、语音参与者列表、个人信息卡片）。只有两态：在线与离开；它不同于连接存活——"离线"（无连接）是成员名单的第三个展示类别，不通过状态设置产生。
_Avoid_: presence、用户状态、在线/离线状态、状态点

**固定离开**：
用户手动选择的在线状态，作为"自动 / 固定离开"状态设置中的一值；设置后自动状态检测暂停，状态保持离开，直到用户手动切回自动模式。
_Avoid_: 手动离开、挂起、隐身

**自动状态检测**：
用户在自动模式下由系统根据说话活动在在线与离开之间自动切换的机制，仅在浏览器已授予麦克风权限时运行；连续 10 分钟未检测到一次连续约 600 毫秒的说话活动则进入离开，检测到新的连续说话活动立即回到在线。静音（含耳机静音）期间说话活动不作为判定依据，离开计时照常进行，解除静音后恢复。阈值固定，不提供用户设置。
_Avoid_: 自动离开、活动检测、在场检测

**自动模式**：
在线状态设置的一值（默认）；在此模式下，状态由自动状态检测在在线与离开之间切换，用户不固定任何一态。
_Avoid_: 默认状态、自动在线

**状态设置**：
用户为自身在线状态选择的模式，两值：自动模式与固定离开；入口位于账户菜单，固定离开持久化在账号上、跨设备生效。
_Avoid_: 状态开关、在线设置、状态管理

**状态点**：
成员名单、语音参与者行、个人信息卡片与账户菜单中表达在线状态的圆点：绿=在线、橙=离开、灰=离线；它表达在场而非连接存活。
_Avoid_: 绿点、在线圆点、presence 指示器

**说话检测引擎**：
登录后浏览器授予麦克风权限即常开的本地说话活动信号源，由一条采集流与一个 VAD 组成，逐帧产出说话事件；在线状态检测与静音说话提醒共享消费，不各自持有采集。它的启停只由登录与麦克风授权驱动，不随任何消费方变化。
_Avoid_: 麦克风监听、VAD、说话识别

## 个人信息卡片

**活跃成员**：
当前服务器中未被该服封禁、也未被平台停用或删除的成员。成员名单仍可能列出被该服封禁的人；斜杠指令目标、提及补全与戳一下只对活跃成员成立。
_Avoid_: 在线成员、有效成员、当前成员

**个人资料**：
用户账号上可供他人查看的展示数据集合，是个人信息卡片的读取来源；由全局字段（头像、显示名、@用户名、简介、平台在线时长、账号创建时间）与可选的服务器上下文字段（服务器语音时长、服务器语音经验、服务器语音等级）组成。全局资料读取要求请求者与目标共享至少一个服务器，平台管理员可绕过该限制；服务器上下文资料要求请求者与目标均为该服务器活跃成员，平台管理员不能绕过成员资格。
_Avoid_: 资料卡、用户资料页、profile、资料接口

**个人信息卡片**：
左键点击成员名单行、消息作者或语音参与者行后弹出的锚定 float，展示该人全局个人信息（头像、显示名、@用户名、简介、平台在线时长、账号创建时间）与当前服务器权限信息（角色、语音/文字禁言、封禁状态与剩余、入服时间、服务器语音时长），并承载同一操作行上的「语音通话」「戳一下」「呼叫屏蔽」——仅当目标为当前服务器活跃成员且非自己时显示（成员对象已在即可，不等资料加载完；资料失败不拿掉按钮）；成员更新为封禁或不再活跃时立刻拿掉戳一下，已发出的请求仍以服务端为准；被该服封禁的成员不显示戳一下；离线活跃成员仍可点，失败走全局错误提示。戳一下从按下到返回前按钮不可点；成功后显示「已戳」并保持不可点直至约 10 秒，不关闭卡片；失败立即恢复可点。关掉再打开卡片可以再看到可点按钮，仍以服务端冷却为准。呼叫屏蔽按钮展开菜单，可选择暂时屏蔽、永久屏蔽或解除。当前用户已屏蔽对方时，呼叫行内展示「已屏蔽呼叫」提示 pill：暂时屏蔽附实时倒计时（剩余 H:MM:SS，每秒跳动，归零即消失），永久屏蔽标注「永久」；它只表达「我屏蔽了对方」，不表达服务器封禁，也不显示「对方屏蔽了我」。所有卡片触发面（成员名单行、语音参与者行、消息作者）都携带 guild 上下文；服务器语音等级与经验条在 header 显示名下展示，与显示名形成"名+衔"衔接。
_Avoid_: 个人资料卡、成员卡、资料弹窗、profile 弹窗、用户卡

**戳一下**：
用户向另一名当前在线的同服活跃成员发出的一次性注意力提醒，从个人信息卡片发起，或在对方被文字消息提及时发放；不落历史、无收件箱、无系统通知、不能拒收、不写审计日志。重连不补发未看完的提示。离线当场失败，不排队不补送。离开可戳；呼叫屏蔽、可被呼叫设置不阻挡；文字禁言不阻挡卡片入口，但挡住发消息，因此禁言期间不能靠提及去戳。卡片请求只带目标用户 id、不带服务器；资格是双方共享至少一个服务器且在该关系里都是活跃成员，与当前正在查看哪个服务器无关；平台管理员不能绕过共享服。提及仍绑定这条消息所在的服务器。同一对发起人→目标每 10 秒最多成功一记（卡片与提及共用），按账号对全局计；发起人另有总闸：10 秒内最多 8 记成功，卡片与提及合计。两道闸都由服务端本进程内存计时、多端共享，仅成功占用；进程重启后归零，不持久化。窗口内再来的丢掉：卡片点下去报「戳得太频繁」，提及则消息照发、不另报。失败不占窗口。已停用、已删除、已封禁即使连接未断也不发放，卡片报「现在不能戳」。一条消息按点名名单从头走到尾发放戳一下，能成功的才占一对人窗口和总闸；离线、已删、已在窗口内的跳过且不占总闸，总闸用尽则后面的人只留消息里的提及。一条消息里每个被点到的人最多一记；消息作者不因自己发出的提及而收到戳一下。该账号每个在线连接都能收到事件；只有已进入主界面的端呈现提示和音效，登录页或加载中丢掉这一记、不排队。音效受提示音总开关、戳一下独立开关与全局耳机静音，不受频道作用域耳机静音——关掉音效或总开关时视觉仍出现。安卓切到后台时不做推送、不另开通道，WebView 还能跑则尽力呈现。发起人成功时只在卡片按钮上看到「已戳」，该态不向其他端同步；失败用全局错误 toast：离线「对方不在线」，冷却「戳得太频繁」，其余资格失败（含戳自己）「现在不能戳」，网络/5xx 走现有操作失败类 toast。带提及的消息发出后不因对方离线再报戳失败。已弹出的提示与音效不因随后删除或清空那条消息而撤回。
_Avoid_: 拍一拍、poke、nudge、戳一戳

**戳一下提示**：
被戳时在主窗口屏幕上方居中出现的短时视觉层，文案「{显示名} 戳了你一下」，不带头像——显示名随账号定向事件带上，为发起当下的全局显示名（目标端成员名单只有当前服务器，不能临时去查另一个服的人）；显示名可见文本单行省略，整句不超过主窗口提示或语音浮层的可用宽度，悬停不展开；读屏读完整显示名 +「戳了你一下」，同一发起人刷新时再播报。约 4 秒，不可点、无按钮，不进入全局 toast 队列；手机上同样居中并避开顶部安全区。叠在 toast 区域下方，不互相遮挡；用户设置面板打开时仍显示，不关面板、不抢焦点。主窗口按发起人去重、最多 3 条，最新在上；同一人刷新只更新那一条并提到最上；超出丢掉最下面最早的。语音浮层只显示最新一句，到期后回到空白或只剩语音参与者，不把上一条再抬出来。通话浮层打开时盖在其上；语音浮层已开启时浮层上同步出现同一句（未加入语音则空白浮层只出这一句，约 4 秒后回到空白）。提示与戳一下音效只由账号定向事件产生。正在查看的文字频道里，实时消息只对接收者自己回带「是否向你发放了戳一下」（不进历史、不对频道里其他人可见）：发放了则只播戳一下、不播新文字消息；未发放则按普通新消息播新文字消息，正文仍用「你被点名」样式。
_Avoid_: 戳一下 toast、提醒条、poke 通知

## 文字消息

**消息编辑器**：
文字频道中用于编写并发送单条消息的输入区域，包括文本输入、字数提示、发送操作，以及以 @ 触发的提及成员补全。仅 ASCII @、且紧前不是登录名合法字符（字母数字 _ -；行首视为满足）时打开提及补全；全角 ＠ 当普通字。提及补全跟光标走：按光标前的文本计算触发位；光标落在已绑定点名段内部时不打开；存在非空选区时不打开、不更新。Esc 关掉后，同一触发位保持关掉，直到该 @ 段被改掉或光标离开再回来。编辑器失焦时提及与斜杠建议都关（点建议项除外），再聚焦不自动打开。输入以 / 开头时整行由斜杠指令接管；斜杠指令仍是改字才刷新建议，Esc 后改字可再出，不跟选区走。提及补全与斜杠指令共用同一套建议列表（不同时开两份），键盘操作一致；打开提及时无障碍名称为「提及建议」，斜杠指令仍为「指令建议」；已满 10 人的空列表可见说明「一条最多 @ 10 人」，辅助技术同样读到。输入法组字过程中两套建议都不打开、不更新，也不拦截 Enter/Tab/方向键，上屏后再算。提及项展示 @登录名与显示名。查询是触发 @ 之后连续的登录名合法字符；空查询列出全部可点的人（活跃成员、不含自己、不含已在待发送名单里的），排序与成员名单相同：角色再显示名。列表不含自己、不含已在待发送名单里的人；查询无命中时建议区收起；名单已满 10 人且仍处于触发态时列表为空，建议区提示「一条最多 @ 10 人」，不插入、不 toast。选中后只替换这个 @ 加这一串查询，插入账号保存的原始登录名写法并跟一个尾随空格；空格不是点名段，删空格不拿掉这条提及。服务端校验正文里的 @登录名时不区分大小写。补全插入的是一段绑定用户的 @登录名，整段必须仍等于插入当下的 @+原始登录名（大小写不敏感），改一个字母即从名单拿掉；掉了之后若光标仍在该触发位，按未绑定查询打开补全。不跟踪账号上登录名后来的写法。改回去须再补全。撤销只还原正文，随后以正文为准重新对齐点名段，对不上的从名单拿掉，不另做撤销栈。建议列表可视区域大约 8 行，超出滚动，不截断条目；键盘上/下循环并把当前项滚进可视区域。手机上高度不超过输入框上方可见区域，必要时少于 8 行，仍贴输入框上沿。查询结束且当前查询为空（例如 @ 后立刻空格）则关掉提及补全；查询非空后再遇到空格只关列表。打开任何 modal（用户设置面板、管理面板、通话浮层）时关掉提及与斜杠建议，组字中也不再弹出；关掉 modal 后不自动再打开。空查询且可点的人为 0 时建议区收起。查询从第 1 个字符起按前缀过滤。草稿按服务器+频道保存在当前浏览器，点名名单与正文共用钥匙；切走再回来按正文对齐，对不上的丢掉，不把 A 频道的名单带到 B。
_Avoid_: 输入框、发送框

**斜杠指令**：
用户在消息编辑器中以单个斜杠开头提交的非消息操作，作用于提交者自身、当前服务器或当前频道，不承载平台级管理。所有用户共用这一交互方式，管理员指令只是其中受权限限制的一类。输入以 / 开头时由斜杠指令接管，其中的 @登录名只是指令参数，不是提及，不发放戳一下。指令建议与提及补全一样，在输入法组字过程中不打开、不更新；失焦时同样关掉。
_Avoid_: 控制台命令、聊天命令、管理员命令

**指令反馈**：
斜杠指令执行后仅向提交者呈现的临时结果，不属于频道消息，也不进入频道历史；需要公开的行为必须显式产生独立的公开效果。
_Avoid_: 指令消息、命令输出、系统消息

**文字禁言**：
服务器管理者对成员施加的公开文字内容发布限制，作用于该服务器的全部文字频道；它不阻止只产生指令反馈的斜杠指令，也不阻止个人信息卡片上的戳一下。被禁言的人不能发出文字消息，因此不能靠提及去戳。
_Avoid_: 禁用消息编辑器、禁用指令、平台禁言

**用户内容**：
展示给频道参与者的他人产出文本，应保留浏览器原生文本选区能力；在界面上由 `data-user-content` 标记。当前主要实例为消息正文（含提及高亮；点名了当前用户的只加强该词，不整行；作者看自己发出的点名全是普通提及高亮）；提及可点但不做成按钮，以免破坏原生选区。不含消息编辑器、输入控件、资料或设置面板等界面 chrome 文本。
_Avoid_: 用户文本、消息文本、内容区

**提及**：
文字消息中对当前服务器活跃成员的结构化点名；仅从消息编辑器的 @ 补全插入才算（匹配登录名或显示名前缀、不区分大小写，插入可见文本为 @登录名），手打同名是普通字符。对象含离线与离开、不含自己、无全体/在线广播；一条最多 10 个。发消息时另带用户 id 列表；服务端不证明来自补全，只校验：当前服务器活跃成员、不是作者、最多 10 个不重复、正文里确有对应 @登录名。通过的随消息保存，读回时带 userId 与登录名；不把点名编码进正文。发送时丢掉通不过的条目，消息本身照发，不另报给发起人。分页加载的更早消息各自携带点名名单，高亮规则与最新消息相同。无名单的历史消息与旧客户端发出的消息不高亮、不补扫。读回时丢掉已删除账号、保留后来退服或被该服封禁的条目。展示时按该条消息的点名名单现算高亮，不另做已高亮缓存；每人只高亮正文里尚未占用的下一个完整 @登录名词（大小写不敏感，@ 后连续登录名合法字符恰好等于该登录名），多出来的手打同名当普通字；目标账号已删除则该条提及不高亮、不可点；退服或被该服封禁仍高亮可点，卡片读失败走现有 404。被提及且当前在线的成员额外收到一记戳一下（按名单从头走，与卡片共用一对人窗口和发起人总闸）；离线或冷却未发放则只有消息中的提及；作者不因自己发出的提及而收到戳一下。正文中的提及是可点的一段字、不是按钮：指针抬起且几乎未移动（未拖出选区）才打开该用户当前服务器上下文的个人信息卡片；长按、拖选、双击、右键复制仍按普通文字。点名了当前用户的只加强该词，不整行；作者看自己发出的点名全是普通提及高亮，名单里若被塞进自己则服务端丢掉且展示不加「你被点名」。不另做「有人提及你」未读样式。
_Avoid_: 艾特、mention、提醒、点名

**清空频道消息**：
服务器管理者对指定文字频道执行的一次性管理操作，删除该频道当前全部消息，频道本体、保留消息数量设置与成员已读位置均不受影响；正在查看该频道的成员立即看到消息清空，未读清零。它与删除频道不同——后者连频道一并永久移除，需输入频道名确认。
_Avoid_: 清除消息、删除消息、清空聊天记录、频道清屏

## 语音音量

**麦克风增益**：
本地用户设定的音量倍率基准，在发送麦克风声音前应用，可在 0%-300% 范围内调节；它不等于最终发布到远端的响度，后者还受采集端自动响度调整动态影响。
_Avoid_: 麦克风音量、输入音量、手动增益、发送电平

**发送电平**：
本地用户实际发布到远端的麦克风响度，由麦克风增益倍率基准、采集端自动响度调整与降噪链路的固定补偿共同作用产生，通话期间可能随说话距离与位置波动。
_Avoid_: 麦克风电平、发送音量、发送增益、实际增益

**扬声器音量**：
本地用户听到的所有远端麦克风和背景音的全局音量倍率，可在 0%-300% 范围内调节；不包含应用提示音。
_Avoid_: 耳机音量、输出增益

**按参与者播放控制**：
本地用户只对某位远端语音参与者设置的麦克风与背景音播放音量或静音状态；音量与静音相互独立，也不会改变该参与者向其他人发送的声音。语音参与者行以黄色图标表达当前用户的本地静音——替代该轨道对方自身状态色且仅当前用户可见，取消本地静音后恢复；它与灰色（对方自身状态）和红色文字「已禁言」（服务器语音禁言）语义分离。
_Avoid_: 用户音量、参与者禁言、服务器静音

**自动音量平衡**：
本地用户开启的全局语音偏好，对每位远端语音参与者的麦克风播放音量按其实测响度自动向统一目标水平收敛——小声的说话人自动放大、大声的自动压低；叠加在"按参与者播放控制"与"扬声器音量"之上，不改变该参与者向他人发送的声音。仅保存在当前浏览器，默认关闭。
_Avoid_: 自动增益、AGC、音量归一化、自动调节音量、声音平衡

## 背景音

**背景音**：
用户在桌面客户端播放的本机应用声音，分享后作为独立于麦克风的音频流发布到语音频道；发布者可暂停/恢复或停止分享，收听者可独立调节其音量或静音。它与麦克风声音分离存在，可同时播放。
_Avoid_: 应用声音、背景音乐、分享音乐、系统声音

**背景音活动**：
语音参与者行中音符指示的亮起状态，表示该参与者分享的背景音当前正在产生真实声音；持续约 5 秒无声音后熄灭（灰色），音符在背景音会话存在期间常显。它只反映分享者背景音的真实声音，亮/灰不受收听者本地静音或耳机静音影响；收听者本地静音时音符整体以黄色呈现，替代亮起/熄灭色（见按参与者播放控制）。
_Avoid_: 音符、音符符号、音乐符号、背景音播放状态

## 语音设备

**降噪选项**：
用户在音频设置或语音操作区中选择的发送端麦克风降噪算法，三值：关闭、系统降噪（WebRTC）、增强降噪（RNNoise），默认增强降噪，仅保存在当前浏览器；只作用于发送到远端的麦克风声音，不影响本地说话检测（VAD）、回声抑制与麦克风增益，切换即时生效。加载失败时本次会话静默回退到系统降噪，设置值不变。
_Avoid_: 降噪模型、降噪开关、噪声抑制、noise suppression

**麦克风发布链编排器**：
Web 前端把麦克风声音发布出去的入口实现载体。频道与语音通话各自一条、调用约定相同：调用方只声明目标状态（启用与否、降噪选项、传输模式），由它决定静音翻转、重建采集与重发布的最小顺序，并内化该条连接上的会话切换与并发竞态防护。
_Avoid_: 发布链、麦克风编排、publish orchestrator

**语音设备管理**：
本机语音设备的权限请求、枚举、偏好持久化、切换（含失败回滚）与缺失降级的实现载体。首选输入/输出设备是浏览器级一份；切换与缺失降级打到当前所有活动语音房间（频道与语音通话），输出切换同时应用到这些房间的音频元素。
_Avoid_: 设备模块、设备 store、音频设备控制

**首选输入设备**：
当前浏览器为后续语音连接优先选用的麦克风设备偏好，不要求用户已经加入语音。
_Avoid_: 当前麦克风、活动输入设备

**首选输出设备**：
当前浏览器为后续语音连接优先选用的扬声器或耳机设备偏好，不要求用户已经加入语音。
_Avoid_: 当前扬声器、活动输出设备

## 语音状态

**语音通话**：
两个同服成员之间临时建立的 1:1 实时语音会话，独立于任何服务器的语音频道；挂断即散、不落历史。通话双方不是「语音参与者」（该词限定频道内）；通话期间频道侧自动进入「频道作用域耳机静音」。发起入口为个人信息卡片的「语音通话」按钮。
_Avoid_: 电话、私聊、DM 通话、1v1 通话、语音私聊

**通话发起**：
主叫请求建立语音通话时的一次性结果分类：资格拒绝、呼叫拒入、不可达、忙碌或振铃。呼叫自己属于输入校验，不是通话发起的结果。
_Avoid_: 发起仲裁、inbound 检查、呼叫预检

**可被呼叫设置**：
用户为自身账号选择的全局通话偏好，决定是否接受他人发起的语音通话呼入；默认开启，关闭后其他人新发起的通话直接结束、不产生振铃，当前已存在的振铃与通话不受影响。它只限制呼入，不影响用户主动呼叫他人。
_Avoid_: 接听开关、来电权限、通话开关、勿扰模式

**呼叫屏蔽**：
用户针对另一用户设置的、阻止对方呼入自己语音通话的单向权限状态；它不阻止用户反向呼叫对方，只影响之后新发起的呼叫，不追溯已存在的振铃与通话，也不阻挡戳一下。只能对与用户共享至少一个服务器的其他用户设置；设置后即使双方暂时不再共享任何服务器，屏蔽仍保留并在重新共享服务器后继续生效。屏蔽有两种：暂时屏蔽与永久屏蔽。
_Avoid_: 拉黑、拒接名单、免打扰名单、通话防火墙

**暂时屏蔽**：
呼叫屏蔽的一种，从设置时刻起 24 小时后自动解除；到期后该用户恢复可被对方呼叫。任何再次变为暂时屏蔽的操作都从新的操作时刻重新计时 24 小时。
_Avoid_: 临时拉黑、短期静音、会话内屏蔽

**永久屏蔽**：
呼叫屏蔽的一种，持续到用户手动解除，不自动过期。
_Avoid_: 永久拉黑、黑名单

**资格拒绝**：
通话发起因双方不共享任何服务器而被拒绝；不建立语音通话、不发通话信令。它不同于呼叫拒入。
_Avoid_: 不能呼叫、无权呼叫、不同服拒绝

**呼叫拒入**：
通话发起因被叫的「可被呼叫设置」关闭或「呼叫屏蔽」而被终止的结果；主叫只看到统一的「对方暂时无法接听」提示，不区分具体原因。
_Avoid_: 呼叫被拒、无法接通、拒接提示

**呼叫屏蔽列表**：
通话设置页中展示用户自己设置的全部暂时屏蔽与永久屏蔽的列表；只显示「我屏蔽的人」，不显示谁屏蔽了我，也不显示未设置屏蔽的其他用户。
_Avoid_: 黑名单、屏蔽名单、拒接列表

**语音参与者**：
已在某个语音频道中建立实时语音连接的服务器成员；仅出现在服务器成员名单中不构成语音参与者。
_Avoid_: 频道用户、语音用户、语音成员

**连接质量**：
语音参与者行右侧实时展示的该参与者语音连接质量，由 LiveKit 服务端按其上行丢包与下行 RTT/抖动/丢包每 5 秒打分，只在质量变化或成员新加入时推送给订阅了该成员轨道的客户端；客户端不自行计算。取值：极佳/良好/较差/连接丢失；「未知」表示尚未收到该成员的质量推送（服务器禁言成员因无发布轨道而永不推送），界面以中性灰点占位，不等同于连接丢失。
_Avoid_: 网络质量、信号强度、信号格、网络状态

**服务器语音禁言**：
服务器管理者对某位成员施加的语音发送限制，对该成员在该服务器内的语音会话生效；它不同于收听者自己的按参与者播放控制。
_Avoid_: 用户静音、本地静音、参与者静音

**语音断开**：
服务器管理者终止某位语音参与者在指定语音频道中当前连接的一次性会话操作；它不追踪该成员后续建立的连接，不改变服务器成员身份，也不阻止该成员立即重新加入语音。
_Avoid_: 踢出频道、语音封禁、移出服务器

**麦克风静音**：
用户主动关闭本机麦克风发布的语音状态偏好，不影响远端音频回放、操作提示音与试听；耳机静音期间会临时连带关闭麦克风，但不覆盖麦克风静音偏好。它独立于服务器语音禁言与按参与者播放控制。
_Avoid_: 输入静音、本地静音、用户静音

**耳机静音**：
用户主动暂停本机麦克风发布与全部本地音频回放的语音状态偏好，同时联动静音语音（频道语音与语音通话）、操作提示音（含戳一下）与静音说话提醒；解除后恢复用户原本的麦克风静音偏好。它独立于服务器语音禁言与按参与者播放控制；「提示音试听」豁免本状态。视觉上的戳一下提示不受本状态影响。

**频道作用域耳机静音**：
通话期间自动施加在用户频道语音连接上的耳机静音，只静音频道（停发频道麦克风、停播频道回放）、不静音通话、不静音戳一下的操作提示音；挂断后自动解除并恢复通话前的静音/耳机静音偏好。它是「耳机静音」的频道作用域变体，与全局耳机静音分离。
_Avoid_: 频道聋、通话静音频道、频道暂离
_Avoid_: 输出静音、静音耳机、本地输出禁用

**语音状态偏好**：
用户希望在下一次加入语音时采用的麦克风静音与耳机静音状态；在线或离线点击对应控制都会更新它。服务器管理员禁言和耳机静音造成的临时连带行为不覆盖该偏好。
_Avoid_: 当前语音状态、服务器语音状态

**静音说话提醒**：
用户已加入语音并主动关闭麦克风后，应用检测到用户正在说话时给出的状态提醒；耳机静音和服务器管理员禁言不属于该提醒。它使用固定双音，不归入操作提示音，不接受系统预置音效或自定义音效。
_Avoid_: 静音检测、管理员禁言提醒

**语音浮层**：
以置顶透明窗口叠加在游戏画面上的只读语音状态展示，展示语音参与者的说话、静音与聋状态——说话与否以整行不透明度区分（默认说话 0.8、未说话 0.4），每个参与者独占一块底色，不显示频道名称；未连接语音时浮窗完全空白。被戳时以只读短暂提示呈现「{显示名} 戳了你一下」（显示名在现有内容宽度内单行省略，不因这一句把窗口撑得更宽；未加入语音、只有这一句时用固定窄宽），浮层已开启即使未加入语音也显示该句（空白浮层只出这一句，约 4 秒后回到空白），仍不可点。该句作为语音浮层状态的可选字段推送，不升桌面壳协议。浮层页面由 Web 前端渲染并按内容上报窗口尺寸，桌面壳承载窗口并应用浮层位置配置。它不提供任何交互，不显示文字频道消息。该能力只在桌面壳中提供，浏览器不提供。

**通话浮层**：
语音通话的居中浮层界面，三个形态：呼出中（取消）、来电（接听/拒绝/暂时屏蔽）、通话中（麦克风静音/挂断与时长）。它是独立 modal，盖在主界面之上；通话期间频道仍在背景（用户未离开频道）。来电态的「暂时屏蔽」同时结束当前来电并对主叫设置暂时屏蔽。通话浮层打开时，戳一下的视觉提示盖在其上，约 4 秒，不可点进资料。
_Avoid_: 通话面板、来电弹窗、电话界面
_Avoid_: Overlay、游戏覆盖层、游戏内浮层、悬浮窗、游戏内语音面板

**浮层配置**：
语音浮层的本机偏好，包括显示大小（相对基准尺寸的百分比缩放）、屏幕位置（以窗口中心点的屏幕百分比坐标，50%/50% 即屏幕正中）与说话/未说话两档不透明度，保存于 Web 端 localStorage，经配置通道实时应用到浮层窗口。
_Avoid_: Overlay 设置、浮层偏好、浮窗设置

**语音操作区**：
界面左侧承载语音控件的区域，含麦克风静音、耳机静音、音量、应用背景音与降噪控制等控件；它是语音操作的一级入口，不承载服务器与账号信息。
_Avoid_: 语音控制区、底部控件条、音频控制区

**频道信息区**：
界面左侧语音操作区上方的语音连接摘要区域，展示当前语音连接状态、所在服务器/频道与码率，并提供断开语音操作；仅加入语音后显示。
_Avoid_: 语音连接面板、连接摘要、频道状态区、频道信息栏

## 语音快捷键

**语音快捷键**：
已登录主界面内以 `Ctrl+Shift` 组合触发的语音操作快捷键家族，包括麦克风静音（`Ctrl+Shift+M`）、耳机静音（`Ctrl+Shift+D`）与浮层快捷键；在任何焦点位置都生效，包括正在消息编辑器打字时，但模态弹窗打开期间不拦截。它是 Web 前端行为，不随浮层窗口提供。
_Avoid_: 全局热键、系统快捷键、键盘命令

**浮层快捷键**：
切换语音浮层启用状态的语音快捷键（`Ctrl+Shift+O`），与语音操作区按钮、设置面板开关拨动同一状态；可在设置面板"语音浮层"页停用，停用偏好仅保存在当前浏览器。
_Avoid_: 浮层热键、Overlay 快捷键、浮层开关快捷键

## 提示音来源

**应用提示音**：
应用为用户操作与状态提醒提供的本地听觉反馈总称，包括操作提示音与静音说话提醒；不含语音通话中他人的麦克风与背景音。
_Avoid_: 提示音、应用音效

**操作提示音**：
为加入语音、退出语音、新文字消息、语音通话与戳一下等本地事件播放的短促反馈音；不含静音说话提醒，也不含语音通话中他人的麦克风与背景音。戳一下使用独立槽位，不与新文字消息共用。正在查看的文字频道里，用接收者私有的实时「是否向你发放了戳一下」在两声之间二选一：发放了只播戳一下，未发放仍播新文字消息。提示与戳一下音效只由账号定向事件产生。用户设置面板音效页为戳一下提供独立开关、系统预置或自定义来源与试听，默认开启，遵守总开关与总音量；关掉总开关或该独立开关只静音，戳一下提示仍出现。
_Avoid_: 通知音、系统音、铃声

**提示音试听**：
用户在设置面板主动播放某操作提示音事件当前选中来源的本地预览动作；遵守提示音总开关与提示音音量，豁免耳机静音（含频道作用域耳机静音），忽略对应事件的独立开关且不受同类提示音限流，只播放该事件最后一次已提交且可播放的来源。
_Avoid_: 试听音效、音效试听、预览提示音

**系统预置音效**：
应用内置、由 Web Audio 振荡器本地合成的提示音；每个事件可从中选用其一，用户不能改写其音符模式，也不能新增。戳一下有单独一组短促音色，听得出与新文字消息不同，不复用消息槽的默认音，也不仿第三方；默认选中该组第一项。
_Avoid_: 默认音效、内置铃声、预置音

**自定义音效**：
用户为本机当前浏览器上传的音频文件，按事件独占一个槽位；不跨事件复用、不上传到服务器、不同步到其他浏览器。只有对应音频可用时才能选为来源；删除或无法使用时，该事件回退到系统预置音效。
_Avoid_: 用户铃声、我的音效、自定义铃声、上传音
