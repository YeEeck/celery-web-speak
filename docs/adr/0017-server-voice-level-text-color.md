# 服务器语音等级文字使用 HSL 颜色映射

个人信息卡片中的服务器语音等级文字需要表达等级高低，但经验条仍只表达当前等级内的进度。决定让等级文字从当前主题强调色开始，使用 HSL 颜色插值在 LV100 到达主题红色；LV100 以上保持红色，异常负数按 LV0 处理。选择 HSL 是为了避免直接在 RGB 空间插值造成的色相路径不符合视觉预期；保留单一封顶值与两个颜色端点，为未来增加等级色阶留下清晰的扩展位置，同时避免建立逐等级颜色表。
