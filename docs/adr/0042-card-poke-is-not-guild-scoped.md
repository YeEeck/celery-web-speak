# 卡片戳一下不带服务器 id

个人信息卡片只在当前服务器活跃成员上显示戳一下，但请求只带目标用户 id。资格是双方共享至少一个服务器、且在该关系里都是活跃成员，与当前正在查看哪个服务器无关。冷却已按账号对全局计，再按服务器过滤会让「在 A 服卡片上戳」和「在 B 服发提及」变成两套窗口。

提及仍绑定消息所在服务器：补全与校验都用那条文字频道的活跃成员。两人只在 A 服是活跃成员、正在 B 服看卡片时，B 的卡片不会出现按钮；从 A 的卡片仍可戳。平台管理员不能绕过共享服——资料全局读取可以，戳一下不行。
