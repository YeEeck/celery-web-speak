# 戳一下提示不走全局 toast

被戳时的视觉是独立的「戳一下提示」层，不进入 toast 队列、不加 info 类型、不可点、约 4 秒。ADR-0002 把 toast 限定为已完成动作的 success/error/warning，并否决过 info；戳一下既不是操作成败，也不该占 3 条上限、不该有关闭按钮。主窗口叠在 toast 区域下方以免互挡；通话浮层打开时盖在通话浮层上；语音浮层另同步一句。发起失败仍用全局错误 toast。
